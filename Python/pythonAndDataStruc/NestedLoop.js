 // Don't change this line

// Execute your function n times with a loop
function CallLoop(n) {
    if (n === 0) {
        return 
    }
  for (let i = 0; i <= n; i++) {
    console.log("500050000");
  }
}
CallLoop(0);
const Multiply = (a, b) => { 
   let  sum =  a * b;   
    console.log(sum);
}
Multiply(5, 6);

let a = 5 // Don't change this line 
const Square = (a) => {
  let square = a * a;
  return square;
};
Square(a);
let result = Square(a);
console.log(result); // Don't change this line